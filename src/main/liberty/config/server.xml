<server description="Server for 12-factor app sample.">

    <featureManager>
        <feature>servlet-3.1</feature>
        <feature>jaxrs-2.0</feature>
        <feature>jsonp-1.0</feature>
        <feature>monitor-1.0</feature>
        <feature>restConnector-1.0</feature>
        <feature>jndi-1.0</feature>
        <feature>cdi-1.2</feature>
    </featureManager>

    <httpEndpoint httpPort="9082" httpsPort="9445"
        id="defaultHttpEndpoint" />

    <webApplication id="12-factor-application" location="12-factor-application-1.0-SNAPSHOT.war" name="12-factor-application"/>

    <applicationMonitor updateTrigger="mbean" />

    <!-- Before enabling this you should configure your trust store for your Cloudant Service Instance -->
    <!-- <quickStartSecurity userPassword="password"
        userName="username"></quickStartSecurity>
    <keyStore password="liberty"></keyStore>-->
    
    <jndiEntry jndiName="cloudant/url" value="${env.CLOUDANT_URL}"/>
    <jndiEntry jndiName="cloudant/username" value="${env.CLOUDANT_USERNAME}"/>
    <jndiEntry jndiName="cloudant/password" value="${env.CLOUDANT_PASSWORD}"/>
</server>
